console.log('\nმაგალითი 8.\n');

function sum(a, b) {
    console.log(a - b > 0 ? "მაქსიმალური ორ რიცხვს შორის არის " + a :
        a - b < 0 ? "მაქსიმალური ორ რიცხვს შორის არის " + b : 'რიცხვები ტოლია', "\n")
}

sum(2, 2);
sum(2, 8);